{
  "name": "@kittycad/web-view",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npx esbuild --bundle src/index.ts --outdir=pkg",
    "serve": "npx esbuild --bundle src/example.ts --outdir=public --servedir=public --serve=localhost:3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@kittycad/lib": "^3.2.2",
    "bson": "^7.2.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "esbuild": "0.27.4"
  }
}
